# Starting Kit

This kit contains a zero-prediction sample submission in `sample_submission/track_b`.

To create a submission zip from the sample:

```bash
cd sample_submission
zip -r track_b_zero_submission.zip track_b
```

Your own submission should contain one file per language named `pred_<lang>.csv`.
